// Remove viewport from metadata export and create separate viewport export

export const viewport = {
  width: 'device-width',
  initialScale: 1,
};

// ...existing code with metadata export without viewport...
